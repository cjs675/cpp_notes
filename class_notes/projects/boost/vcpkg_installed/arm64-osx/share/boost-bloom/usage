The package boost-bloom is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS bloom)
    target_link_libraries(main PRIVATE Boost::bloom)

or the generated cmake configs via:

    find_package( REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::bloom)
